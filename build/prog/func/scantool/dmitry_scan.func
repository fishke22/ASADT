function scantool_dmitryscan {

    if [ "$scantool_dmitry_enable" -eq 1 ]; then

        sudo dmitry -winsepo $dmitryscan_default_output $targetip

    else

        echo "dmitry_scan.func> Either no Target was provided, or this scan is disabled"

    fi

}
